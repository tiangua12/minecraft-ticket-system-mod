# 1. 进入你的项目目录（请替换为你的实际路径）
cd /storage/emulated/0/MDKmod/MOD工作区/火车票

# 2. 初始化本地Git仓库（只做一次）
git init

# 3. （Android设备特需）将此目录标记为安全，避免权限警告
git config --global --add safe.directory “$(pwd)”

# 4. 配置你的用户名和邮箱（与GitHub关联）
git config user.name “tiangua12”
git config user.email “3668939899@qq.com”